# Skyline Frontend

Frontend-only Next.js project for the shared CRM backend architecture.

## Run
npm install
npm run dev

Open http://localhost:3000

## Shared backend
Set NEXT_PUBLIC_API_BASE_URL in .env.local when connecting the shared backend.
This repository contains no Prisma schema, database, or backend folder.
