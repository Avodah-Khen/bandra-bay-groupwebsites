import Image from 'next/image';
export type AmenityData={id:string;name:string;category:string;imageUrl:string|null};
export default function AmenityGrid({amenities}:{amenities:AmenityData[]}){if(!amenities.length)return <p className="text-sm text-ink/50">Amenities will appear here once added in the admin CMS.</p>;return <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4">{amenities.map(a=><div key={a.id} className="card overflow-hidden"><div className="relative aspect-[4/3] bg-ink/5">{a.imageUrl?<Image src={a.imageUrl} alt={a.name} fill sizes="(max-width:768px) 50vw, 25vw" className="object-cover"/>:<div className="flex h-full items-center justify-center text-gold">●</div>}</div><div className="p-4"><p className="text-sm font-medium">{a.name}</p><p className="mt-1 text-xs text-ink/40">{a.category}</p></div></div>)}</div>}
