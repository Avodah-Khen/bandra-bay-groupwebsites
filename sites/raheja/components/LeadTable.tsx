"use client"; import {useState} from "react"; const statuses=["NEW","CONTACTED","QUALIFIED","SITE_VISIT","NEGOTIATION","CONVERTED","LOST"]; export default function LeadTable({leads}:{leads:any[]}){const [rows,setRows]=useState(leads);async function update(id:string,status:string){const r=await fetch("/api/admin/leads",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});if(r.ok)setRows(x=>x.map((l:any)=>l.id===id?{...l,status}:l));else alert("Could not update lead");}return <div className="tablewrap"><table><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Project</th><th>Status</th><th>Source</th><th>Created</th></tr></thead><tbody>{rows.map((l:any)=><tr key={l.id}><td>{l.name}</td><td>{l.phone}</td><td>{l.email||"-"}</td><td>{l.project?.title||"General"}</td><td><select value={l.status} onChange={e=>update(l.id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td>{l.source}</td><td>{new Date(l.createdAt).toLocaleString()}</td></tr>)}</tbody></table></div>}