import {redirect} from "next/navigation"; import Link from "next/link"; import {getAdmin} from "@/lib/auth"; import {prisma} from "@/lib/prisma";
export default async function AdminProjects(){if(!(await getAdmin()))redirect("/admin/login");const projects=await prisma.project.findMany({orderBy:{createdAt:"desc"},include:{_count:{select:{units:true}}}});return <main className="admin"><div className="adminnav"><b>RAHEJASPACES / PROJECTS</b><Link href="/admin">Dashboard</Link></div><div className="adminwrap"><h1>Projects</h1><div className="tablewrap"><table><thead><tr><th>Title</th><th>Category</th><th>Status</th><th>Published</th><th>Units</th></tr></thead><tbody>{projects.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.category}</td><td>{p.status}</td><td>{p.published?"Yes":"No"}</td><td>{p._count.units}</td></tr>)}</tbody></table></div></div></main>}
