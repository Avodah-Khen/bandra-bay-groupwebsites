import {redirect} from "next/navigation"; import {getAdmin} from "@/lib/auth"; import {prisma} from "@/lib/prisma"; import LeadTable from "@/components/LeadTable"; export default async function Leads(){const admin=await getAdmin();if(!admin)redirect("/admin/login");const leads=await prisma.lead.findMany({include:{project:true},orderBy:{createdAt:"desc"}});return <main className="admin"><div className="adminnav"><b>RAHEJASPACES CRM / LEADS</b><a href="/admin">Dashboard</a></div><div className="adminwrap"><h1>Leads</h1><p className="meta">Signed in as {admin.role}</p><LeadTable leads={leads}/></div></main>}