# RahejaSpaces Frontend

Contains public pages, admin UI pages, React components, styles and project assets.

Important: the original app is a Next.js full-stack application and several server components directly import `@/lib/prisma`. This is therefore a logical source separation, not yet an independently runnable frontend. To make it a true frontend service, replace direct Prisma calls with HTTP requests to the backend.
