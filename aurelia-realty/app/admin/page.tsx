'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {apiJson} from '@/lib/api';
type A={total:number;properties:number;converted:number};
function Sidebar(){return <aside className="sidebar"><div className="brand">AURELIA</div><p className="muted">Control Center</p><a href="/admin">Dashboard</a><a href="/admin/properties">Properties</a><a href="/admin/leads">Leads & CRM</a><a href="/">View website</a></aside>}
export default function Admin(){const [a,setA]=useState<A|null>(null);const [email,setEmail]=useState('');const router=useRouter();useEffect(()=>{apiJson<A>('/api/analytics').then(setA).catch(()=>router.push('/admin/login'));},[router]);if(!a)return <div className="section"><div className="container">Loading admin…</div></div>;return <div className="admin"><Sidebar/><main className="adminmain"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div className="eyebrow">Admin</div><h1 className="serif">Dashboard</h1></div><span>{email}</span></div><div className="metricgrid"><div className="metric"><span className="muted">Total leads</span><br/><b>{a.total}</b></div><div className="metric"><span className="muted">Published properties</span><br/><b>{a.properties}</b></div><div className="metric"><span className="muted">Converted</span><br/><b>{a.converted}</b></div></div><section className="section" style={{paddingBottom:0}}><h2 className="serif">Operations</h2><div className="grid cards"><Link className="property-card" href="/admin/properties" style={{padding:25,textDecoration:'none',color:'inherit'}}>Manage property inventory →</Link><Link className="property-card" href="/admin/leads" style={{padding:25,textDecoration:'none',color:'inherit'}}>Work the CRM pipeline →</Link></div></section></main></div>}
