'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {apiJson} from '@/lib/api';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');const b=Object.fromEntries(new FormData(e.currentTarget));try{await apiJson('/api/auth/login',{method:'POST',body:JSON.stringify(b)});router.push('/admin')}catch{setError('Invalid email or password')}}return <main className="section"><div className="container" style={{maxWidth:500}}><div className="eyebrow">Secure admin</div><h1 className="serif">Sign in</h1><form onSubmit={submit} className="adminform"><input name="email" type="email" placeholder="Email" defaultValue="admin@aurelia.local" required/><input name="password" type="password" placeholder="Password" defaultValue="Admin@12345" required style={{marginTop:10}}/><button className="btn" style={{marginTop:15}}>Sign in</button>{error&&<p className="error">{error}</p>}</form><p className="muted">Development credentials: admin@aurelia.local / Admin@12345. Change them before production.</p></div></main>}
