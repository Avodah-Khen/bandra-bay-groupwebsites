'use client';
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {apiJson} from '@/lib/api';
function Sidebar(){return <aside className="sidebar"><div className="brand">AURELIA</div><a href="/admin">Dashboard</a><a href="/admin/properties">Properties</a><a href="/admin/leads">Leads & CRM</a></aside>}
export default function LeadsAdmin(){const [items,setItems]=useState<any[]>([]);const router=useRouter();useEffect(()=>{apiJson<any[]>('/api/leads').then(setItems).catch(()=>router.push('/admin/login'))},[router]);async function status(id:string,status:string){try{await apiJson(`/api/leads/${id}`,{method:'PATCH',body:JSON.stringify({status})});setItems(items.map(x=>x.id===id?{...x,status}:x))}catch(e){alert(e instanceof Error?e.message:'Update failed')}}return <div className="admin"><Sidebar/><main className="adminmain"><div className="eyebrow">CRM</div><h1 className="serif">Lead pipeline</h1><table className="table"><thead><tr><th>Lead</th><th>Contact</th><th>Property</th><th>Status</th><th>Created</th></tr></thead><tbody>{items.map(l=><tr key={l.id}><td><b>{l.name}</b><br/><span className="muted">{l.source}</span></td><td>{l.phone}<br/>{l.email||''}</td><td>{l.property?.title||'General enquiry'}</td><td><select value={l.status} onChange={e=>status(l.id,e.target.value)}><option>NEW</option><option>CONTACTED</option><option>QUALIFIED</option><option>SITE_VISIT</option><option>NEGOTIATION</option><option>CONVERTED</option><option>LOST</option></select></td><td>{new Date(l.createdAt).toLocaleString()}</td></tr>)}</tbody></table></main></div>}
